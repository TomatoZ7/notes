<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cbh.dao.UserDao">
    <resultMap type="User" id="user">
        <id property="id" column="id" />
        <result property="name" column="name" />
        <result property="password" column="password" />
        <result property="account" column="account" />
        <result property="gender" column="gender" />
        <result property="phone" column="phone" />
		<result property="avatar" column="avatar" />
		<result property="status" column="status" />
		<result property="has_rights" column="has_rights" />
		<result property="create_time" column="create_time" />
    </resultMap>
    <!-- 对于数据表与实体类属性不一致情况，使用resultMap进行处理 -->
    <select id="getUserById" resultMap="user">
        SELECT * FROM user WHERE id=#{id};
    </select>
    <delete id="delUserById">
        DELETE FROM user WHERE id = #{stid};
    </delete>
    <update id="updateUser" parameterType="Map">
        UPDATE user
        <set>
            <if test="password != null">
                password = #{password},
            </if>
            <if test="name != null">
                name = #{name}
            </if>
        </set>
        where id = #{id};
    </update>
    <insert id="insertUser" parameterType="User">
        INSERT INTO
        user(name,password,account,gender,phone,avatar,status,has_rights,create_time)
        VALUES(#{name},#{password},#{account},#{gender},#{phone},#{avatar},#{status},#{has_rights},#{create_time});
    </insert>
</mapper>